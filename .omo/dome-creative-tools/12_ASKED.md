---
name: "12_ASKED.md"
description: "Use this file as the honest record of what remains unresolved in DOME Creative Tools. Every [OPEN] item is a question that must be answered before or during implementation. Do not hallucinate answers — if something is unknown, mark it [OPEN] and move forward with the safest assumption."
---

# 12_ASKED.md — DOME Creative Tools
> Generated by: Ritroso.md v3 skill
> Domain: web-creative-tools
> Status: RITROSO-VERIFIED
> Date: 2026-08-24
> Folder: new-ideas/web-creative-tools/dome-creative-tools/
> Inference loop: COMPLETE — 12 resolutions, 4 open
> Set: [[00_INDEX]] [[01_GOAL]] [[02_PRODUCT]] [[03_NEXT_STEPS]]
>      [[04_ELEMENTS]] [[05_COMPONENTS]] [[06_PRICE]] [[07_BUDGET]]
>      [[08_LIMITS]] [[09_AGENTS]] [[10_ERROR]] [[11_INTERPOLATION]] [[12_ASKED]]

## UNRESOLVED QUESTIONS

### [OPEN-1] Which specific tools to build first?

**Status**: Partially resolved — MVP is audio-reactive visualizer, but visual modes (bars, particles, waveform, shader) are not prioritized.

**Safest assumption**: Start with frequency bars (simplest Canvas 2D implementation), add waveform as second mode. Particles and shaders are P3.

**Needs answer from**: Domenico

---

### [OPEN-2] How to handle the DOME static site + React app coexistence?

**Status**: Partially resolved — React app lives in `tools-app/` subfolder, built separately, deployed as the Pages artifact. But the existing static `index.html` at root needs to coexist.

**Safest assumption**: Either (a) React app replaces the entire Pages artifact (DOME static site moves to a different hosting), or (b) React app is built into a subpath and the static site remains at root. Option (b) requires a combined artifact or a redirect.

**Needs answer from**: Domenico — does the existing DOME static site stay on Pages, or move elsewhere? If it stays, the React app needs to be built into a subdirectory of the static site, not replace it.

---

### [OPEN-3] ffmpeg.wasm bundle size vs GitHub Pages 1GB limit

**Status**: Open — ffmpeg.wasm core is ~30MB, full build is ~100MB+. If bundled in the main chunk, it could push the artifact over 1GB when combined with other assets.

**Safest assumption**: Load ffmpeg.wasm dynamically with `import()` only when the video tool is accessed. It won't be in the initial bundle. Verify total artifact size after build.

**Needs answer from**: Domenico — is the video tool a priority? If not, defer ffmpeg.wasm entirely.

---

### [OPEN-4] Mobile audio permissions UX

**Status**: Open — iOS Safari has strict audio policies. AudioContext requires explicit user gesture. Mic access requires HTTPS (Pages provides this) and permission prompt.

**Safest assumption**: Show a clear "Tap to start" button. On mobile, prefer file upload over mic (mic on mobile web is unreliable). Test on iOS Safari and Android Chrome.

**Needs answer from**: Testing on real devices.

---

## RESOLVED QUESTIONS (from inference loop)

| Question | Resolution |
|----------|------------|
| Can React run on GitHub Pages? | Yes, with Vite build + Actions workflow |
| How to inject Firebase config? | VITE_FIREBASE_* env vars in Actions, read via import.meta.env |
| Which router to use? | HashRouter (BrowserRouter breaks on Pages refresh) |
| What's the hosting cost? | €0 (GitHub Pages + Firebase Spark) |
| Is a backend needed? | No — all processing client-side |
| How to handle audio autoplay policy? | Create AudioContext inside user gesture handler |
| Can the existing DOME site coexist? | [OPEN-2] — needs architecture decision |

## WHAT THE USER ASKED (original prompt summary)

Domenico wants to create a series of tools for images, video, text, and audio-reactive components, inspired by tools.sketchdesign.club, hosted on GitHub Pages inside the existing DOME repo. He asked to generate development files using the Ritroso.md skill, with everything going into the DOME site repo.
